import { Mail, Phone, Truck, UserRound } from 'lucide-react';
import AppShell from '../../components/AppShell';
import { initials, useAuthUser } from '../../context/AuthUserContext';
export default function Profile() { const user = useAuthUser(); return <AppShell><div className="page-heading"><div><span className="eyebrow purple">Account</span><h1>Profile</h1><p>Manage your dispatcher and fleet information.</p></div></div><section className="profile-card"><div className="profile-avatar">{initials(user?.name)}</div><h2>{user?.name || 'Loading profile…'}</h2><span className="profile-role">{user?.role || 'Fleet dispatcher'}</span><div className="profile-fields"><Info icon={<Mail />} label="Email" value={user?.email || 'Loading…'} /><Info icon={<Phone />} label="Mobile" value={user?.mobile_number || 'Not provided'} /><Info icon={<Truck />} label="Fleet" value="North East Transit · 12 vehicles" /></div><button className="button">Edit profile</button></section></AppShell>; }
function Info({ icon, label, value }) { return <div><span>{icon}</span><small>{label}</small><strong>{value}</strong></div>; }
